export const selectRootPath = (state: { rootPath: string }) => state.rootPath;
export const selectUser = (state: { user: any }) => state.user;
export const selectToken = (state: { token: string | null }) => state.token;
